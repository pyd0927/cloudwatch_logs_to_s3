[project]
name = "cloudwatch-log-to-s3"
version = "0.1.0"
description = "AWS CloudWatch Log to S3 Lambda function for backup and retention management"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "boto3==1.34.0",
    "botocore==1.34.0",
    "python-dateutil==2.8.2"
]
